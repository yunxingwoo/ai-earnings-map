// ============================================================
//  小程序配置 —— 部署前请修改这一项
// ============================================================
//
//  把 WEBVIEW_URL 换成「你自己可控域名」下部署好的站点首页地址。
//
//  硬性要求：
//    1) 必须是 https
//    2) 该域名必须在 微信公众平台 → 开发 → 开发管理 → 开发设置 → 业务域名
//       中完成校验（把微信给你的 MP_verify_xxxx.txt 放到该域名根目录，
//       可被 http/https 直接访问）
//    3) 域名须为你自己拥有并可控。
//       ⚠️ WorkBuddy 沙箱 *.app.workbuddy.link 不行 —— 你无法在其根目录
//          放置校验文件，因此不能加入业务域名白名单。
//          （该链接适合"生成二维码 → 微信里点开浏览器看"，但不能嵌进小程序）
//
//  推荐托管方式：EdgeOne Pages（连接自定义域名，免费、CDN、不休眠），
//  把本仓库的 site/ 目录整体部署上去即可得到下面的地址。
//
module.exports = {
  WEBVIEW_URL: 'https://YOUR_DOMAIN_HERE/index.html'
}
